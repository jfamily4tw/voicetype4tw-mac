# VoiceType4TW-Mac 依賴清單
# 使用方式: pip install -r requirements.txt

# === 核心 ===
PyQt6>=6.6
rumps>=0.4.0
sounddevice>=0.4.6
pynput>=1.7.6
pyperclip>=1.8.2
httpx>=0.25
certifi

# === 語音辨識引擎 ===
faster-whisper>=1.0
numpy

# === MLX (Apple Silicon 加速, 選用) ===
# MLX 必須鎖在 <0.30：MLX 0.30+ wheel = macosx_26_0_arm64 + MSL 4.0 metallib，
# 只跑 macOS 26+；對 macOS 13/14/15 使用者啟動會 RuntimeError 載不到 Metal kernel。
# 詳見 openspec/specs/mlx-version-pin/spec.md。
# 要升級必須開新 Spectra change 並在 design.md 解釋是否放棄 macOS 13/14/15 支援。
mlx>=0.29,<0.30
mlx-whisper>=0.4

# === LLM (選用) ===
openai>=1.0
opencc-python-reimplemented>=0.1.7

# === macOS 框架橋接 ===
pyobjc-core
pyobjc-framework-Cocoa
pyobjc-framework-Quartz
pyobjc-framework-ApplicationServices
pyobjc-framework-CoreText
